# .gitignore

# 1. Ignoriere die Datenbank und private Daten
# Wir wollen nicht, dass User-Daten auf GitHub landen!
private_data/
data/
*.db
*.sqlite

# 2. Ignoriere System-Dateien
.DS_Store
Thumbs.db

# 3. Ignoriere Logs
logs/
*.log

# 4. Optional: Ignoriere die echte Config, falls dort Passwörter stehen
# (Da wir SQLite nutzen, ist config.php oft harmlos, aber bei MySQL wäre es kritisch)
# includes/config.php
